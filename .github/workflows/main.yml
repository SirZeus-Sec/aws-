name: TruffleHog Secret Scan

on:
  pull_request:
  workflow_dispatch:

jobs:
  scan:
    name: trufflehog
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install TruffleHog
        run: pip install trufflehog

      - name: Get Changed Files in PR
        id: changed_files
        run: |
          git fetch origin ${{ github.event.pull_request.base.ref }}
          BASE_COMMIT=$(git merge-base origin/${{ github.event.pull_request.base.ref }} HEAD)
          
          # Get only added or modified files
          git diff --diff-filter=AM --name-only $BASE_COMMIT HEAD > changed_files.txt
          
          if [ ! -s changed_files.txt ]; then
            echo "No changed files to scan."
            exit 0
          fi

          - name: Get Changed Files in PR
            id: changed_files
        run: |
          BASE_COMMIT=$(git merge-base origin/${{ github.event.pull_request.base.ref }} HEAD)

          git diff --diff-filter=AM --name-only $BASE_COMMIT HEAD > changed_files.txt

          if [ ! -s changed_files.txt ]; then
            echo "No changed files to scan."
            exit 0
          fi

      - name: Post TruffleHog Results as PR Comment
        if: failure()
        env:
          GITHUB_TOKEN: ${{ secrets.mu_GITHUB_TOKEN }}
        run: |
          COMMENT="🚨 TruffleHog found secrets in this PR! Please check the logs."
          PR_NUMBER="${{ github.event.pull_request.number }}"
          curl -s -X POST -H "Authorization: token $GITHUB_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"body\": \"$COMMENT\"}" \
            "https://api.github.com/repos/${{ github.repository }}/issues/${PR_NUMBER}/comments"
